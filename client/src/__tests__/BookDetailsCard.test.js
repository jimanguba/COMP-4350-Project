test('Empty fields result in those fields being hidden', () => {

})

test('Clicking edit button results in "editing" state change', () => {

})

test('editing state being true results in enabled inputs', () => {

})

test('editing state being false results in disabled inputs', () => {

})