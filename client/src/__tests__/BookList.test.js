test('Entering character into search bar results in "query" state change', () => {

})

test('Non-empty query results in matching titles shown', () => {

})

test('Non-matching query results in "no match" output', () => {

})